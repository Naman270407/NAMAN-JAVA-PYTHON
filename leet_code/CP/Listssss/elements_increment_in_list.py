# generate a new list which contains all value of original array increased by increment value.

print("Enter the elements of the list : ")
a = list(map(int, input().split()))
b = []
for i in range(0,len(a)):
    b.append(a[i]+1)
print(b)


# odd even
# square
